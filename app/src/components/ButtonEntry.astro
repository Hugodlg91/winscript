---
interface Props {
  title: string;
  buttonTitle: string;
  buttonId: string;
  icon?: string;
  description?: string;
}

const { title, buttonTitle, buttonId, icon, description } = Astro.props;
---

<div class="content-entry">
  <div>
    {icon && <img src={icon} alt={title} />}
    <div>
      <h1 data-i18n={title}>{title}</h1>
      {description && <p data-i18n={description}>{description}</p>}
    </div>
  </div>
  <button id={buttonId} data-i18n={buttonTitle}>{buttonTitle}</button>
</div>
